[package]
name=array
version=1.0.0
include=libarray
description=Array manipulation functions, such as joining an array into a string and seeing if a value exists.

[dependencies]
0=assign

[devDependencies]
0=embed-docs
1=unittest

[scripts]
readme=embed-docs README.md libarray
test=unittest test/*.test
